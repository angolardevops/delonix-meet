# Rust
server/target/

# Node
web/node_modules/
web/dist/

# Flutter
mobile/build/
mobile/.dart_tool/

# Env / secrets
.env
*.local
deploy/delonix.env
# Deploy 12-factor: config real e artefactos gerados sem intervenção humana
# (segredos idempotentes, certificados, kubeconfig). NUNCA versionar.
deploy/config.yml
deploy/ansible/.secrets/
deploy/ansible/.generated/
deploy/ansible/*.retry
deploy/ansible/fetched/

# Assets de IA self-hosted pesados — FORA do git (obtidos por deploy.sh /
# Dockerfile / deploy/fetch-whisper.sh, com SHA-256 fixado). Ver
# docs/reference/architecture.md. Os .tflite do MediaPipe (fallback, ~2 MB)
# ficam versionados — não têm script de fetch.
web/public/ort/
web/public/ort-rvm/
web/public/models/rvm/
web/public/models/Xenova/

# Worktrees transitórios de agentes
agents/worktrees/

# OS
.DS_Store

# TypeScript build cache
*.tsbuildinfo

# Gravações de reuniões (dados de utilizador — nunca versionar)
server/recordings/
.dev/
voice/tls/
voice/kamailio/ao_trunk.txt

# Preview local (path da máquina)
agents/launch.json
backups/

# Credenciais de ambiente (12-factor — nunca no repo)
deploy/ansible/.env.*
deploy/ansible/vault/

# Configuração local de ferramentas de agente (fica na máquina, não no repo).
.claude/
CLAUDE.md
