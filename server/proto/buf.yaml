# Contratos gRPC internos do Delonix Meet (ADR-0004 §4, ADR-0006 §3).
# `buf lint` e `buf breaking` correm no CI contra a origin/main.
version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
breaking:
  use:
    - WIRE_JSON
