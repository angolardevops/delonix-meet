# Web image para stage/kind: usa o dist já compilado localmente.
# Evita npm ci dentro do Docker (pode falhar por falta de memória no builder).
FROM nginxinc/nginx-unprivileged:1.27-alpine
COPY deploy/k8s/nginx.conf /etc/nginx/conf.d/default.conf
COPY web/dist /usr/share/nginx/html
EXPOSE 8080
